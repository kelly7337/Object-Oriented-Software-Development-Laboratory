Studio 21 ReadMe.txt 

1. Ermu Lu

2. Declaring the base class destructor as virtual ensures the call of the derived class destructor, when an object 
   of the derived class type is destroyed via a pointer or a reference of the base class type.

3. The usefulness of Dependency Injection is in that the implementation of a class is more flexible and 
   independent by decreasing the amount of coupling between a client and another service.Command prompt is only
   injected with concrete command, file system, and file factory objects. So it is flexible and reusable.
   Yes, it can.

4. I successfully created a TouchCommand, and then I tested addFile. I successfully run the command prompt.
   The program worked as expected.